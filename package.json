{
    "name": "source-rpc-workspace",
    "private": true,
    "description": "Source RPC and its contract-extraction CLI",
    "type": "module",
    "license": "MIT",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "npm run build --workspace=@source-repo/rpc && npm run build --workspace=@source-repo/rpc-cli && npm run build --workspace=@source-repo/queue",
        "clean": "npm run clean --workspaces",
        "test": "npm run test --workspace=@source-repo/rpc && npm run test --workspace=@source-repo/rpc-cli && npm run test --workspace=@source-repo/queue",
        "typecheck": "npm run typecheck --workspace=@source-repo/rpc && npm run typecheck --workspace=@source-repo/rpc-cli && npm run typecheck --workspace=@source-repo/queue",
        "lint": "eslint . --max-warnings 0"
    },
    "devDependencies": {
        "@eslint/js": "^10.0.1",
        "eslint": "^10.8.0",
        "globals": "^15.0.0",
        "typescript-eslint": "^8.0.0"
    }
}
